import React from 'react';

const Header = () => {
    return (
        <section className="header navbar bg-primary">
            <h1>Comment keeper</h1>
        </section>
    );
};

export default Header;
